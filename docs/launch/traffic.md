# Orbit Phase 1 — Traffic (quality)

Sequence after Signet leaf when possible. No Fiverr traffic. No PH-as-strategy.

## Queries (one-stack)

1. Deploy Cloudflare Workers + Vercel together checklist  
2. `wrangler` + frontend env `VITE_API_URL` wire-up  
3. Indie deploy portal vs memorizing four CLIs  
4. Read deploy failure logs agent-friendly  
5. `orbit doctor` before first production push  

## Metrics

Hook views (vanity) · **doctor/ship demo completes** (quality) · sample opens · checkout starts · paid · junk→kill channel.

Funnel: `HOOK → GIFT CLI → sample → CONVERT → soft-no`.
